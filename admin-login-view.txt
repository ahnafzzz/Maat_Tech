```blade
{{-- resources/views/admin/auth/login.blade.php --}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Access Terminal | MechArm Lumina</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        body { 
            background-color: #0a0a0f; 
            font-family: 'Inter', sans-serif;
        }
        .font-mono { font-family: 'JetBrains Mono', monospace; }
        .grid-bg {
            background-image: linear-gradient(to right, #1e1e2e 1px, transparent 1px),
                              linear-gradient(to bottom, #1e1e2e 1px, transparent 1px);
            background-size: 40px 40px;
            mask-image: linear-gradient(to bottom, transparent, black 20%);
 }
        .cursor-blink {
            animation: blink 1s step-end infinite;
        }
        @keyframes blink { 50% { opacity: 0; } }
        .terminal-input {
            background: transparent;
            caret-color: #14b8a6;
        }
        .terminal-input:focus {
            outline: none;
            box-shadow: 0 1px 0 0 #14b8a6;
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center relative overflow-hidden">
    <div class="fixed inset-0 grid-bg opacity-40"></div>
    
    {{-- Ambient glow --}}
    <div class="absolute top-1/4 left-1/2 -translate-x-1/2 w-96 h-96 bg-teal-900/20 rounded-full blur-3xl"></div>

    <div class="relative z-10 w-full max-w-md p-8">
        {{-- Terminal Header --}}
        <div class="glass-panel border border-gray-800 rounded-t-lg bg-gray-900/80 backdrop-blur-sm p-3 flex items-center space-x-2 border-b border-gray-800">
            <div class="flex space-x-1.5">
                <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
                <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
                <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
            </div>
            <div class="flex-1 text-center">
                <span class="font-mono text-xs text-gray-500">sys.term://admin_auth</span>
            </div>
        </div>

        {{-- Login Body --}}
        <div class="bg-gray-950/90 backdrop-blur-md border-x border-b border-gray-800 rounded-b-lg p-8 shadow-2xl">
            <div class="mb-8 text-center">
                <div class="inline-flex items-center justify-center w-12 h-12 bg-teal-900/30 border border-teal-700/50 rounded mb-4">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 text-teal-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
                    </svg>
                </div>
                <h2 class="font-mono text-lg text-white tracking-wider">AUTHENTICATION_REQUIRED</h2>
                <p class="text-xs text-gray-500 mt-2 font-mono">Enter clearance credentials to proceed</p>
            </div>

            @if ($errors->any())
                <div class="mb-6 p-3 bg-red-900/20 border border-red-800/50 rounded text-red-400 text-xs font-mono">
                    [ERROR] {{ $errors->first() }}
                </div>
            @endif

            <form method="POST" action="{{ route('admin.login') }}" class="space-y-6">
                @csrf
                
                <div>
                    <label class="block text-xs font-mono text-teal-500 mb-2 tracking-wider">ADMIN_ID //</label>
                    <div class="relative">
                        <span class="absolute left-3 top-2.5 text-gray-600 font-mono text-sm">#</span>
                        <input 
                            type="text" 
                            name="admin_id" 
                            value="{{ old('admin_id') }}"
                            placeholder="ADM-7701-X"
                            class="terminal-input w-full bg-gray-900/50 border border-gray-800 rounded pl-8 pr-4 py-2.5 text-white font-mono text-sm placeholder-gray-600 focus:border-teal-600 transition-colors"
                            required
                        >
 </div>
                </div>

                <div>
                    <label class="block text-xs font-mono text-teal-500 mb-2 tracking-wider">PASSKEY //</label>
                    <div class="relative">
                        <input 
                            type="password" 
                            name="password"
                            placeholder="••••••••••••"
                            class="terminal-input w-full bg-gray-900/50 border border-gray-800 rounded px-4 py-2.5 text-white font-mono text-sm placeholder-gray-600 focus:border-teal-600 transition-colors"
                            required
                        >
                    </div>
                </div>

                <div class="flex items-center justify-between">
                    <label class="flex items-center space-x-2">
                        <input type="checkbox" name="remember" class="rounded bg-gray-900 border-gray-700 text-teal-600 focus:ring-teal-600/50">
                        <span class="text-xs text-gray-500 font-mono">PERSIST_SESSION</span>
                    </label>
                </div>

                <button type="submit" class="w-full bg-teal-700 hover:bg-teal-600 text-white font-mono text-sm py-3 rounded border border-teal-500/50 transition-all flex items-center justify-center space-x-2 group">
                    <span>EXECUTE_LOGIN</span>
                    <span class="cursor-blink text-teal-300">_</span>
                </button>
            </form>

            <div class="mt-8 pt-6 border-t border-gray-800 flex justify-between items-center">
                <span class="text-[10px] text-gray-600 font-mono">SECURE_CONNECTION</span>
                <div class="flex items-center space-x-1">
                    <span class="w-1.5 h-1.5 bg-green-500 rounded-full animate-pulse"></span>
                    <span class="text-[10px] text-green-500/80 font-mono">ENC: TLSv1.3</span>
                </div>
            </div>
        </div>
        
        <div class="text-center mt-6">
            <a href="/" class="text-xs text-gray-600 hover:text-teal-500 font-mono transition-colors">&lt; RETURN_TO_MAINFRAME</a>
        </div>
    </div>
</body>
</html>
```